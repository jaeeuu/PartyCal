

export default function VotePageIndex() {
  // const params = useParams();
  // navigator.userAgent.includes('KAKAOTALK')

  return (
    <div>
      SEARCH AND JOIN VOTE
      {/* <input
        {...stylex.attrs(ixStyles.sliderBox)}
        type="range"
        min="0"
        max="100"
        value="0"
      /> */}
    </div>
  );
}